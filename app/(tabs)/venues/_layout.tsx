import { Stack } from "expo-router";

const VenueLayout = () => {
  return (
    // AuthProvider goes here
    <Stack 
      screenOptions={{
        headerShown:false
      }}>
    </Stack>
  )
};

export default VenueLayout;
